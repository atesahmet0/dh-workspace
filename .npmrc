# Pin exact versions on `pnpm add` so future installs stay on the current
# @deepseek-ai/* release lines. A bare `latest` for most @deepseek-ai/dsh-*
# packages resolves the STALE 0.0.1-rc.1 line; only the pinned 0.1.0-rc.6
# (`next`) line matches the deployed @deepseek-ai/dsh CLI.
save-exact=true
